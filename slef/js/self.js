//封装一些常用得方法

//计数，类似countUp

var self = function(){
    self.count = function(dom,start,end,time){
        var dom = $(dom).
        var start = Number(start);
        var end = Number(end);
        var time = Number(time);
        var duration = Math.abs(end - start)/time;
        var timer = setTimeout(function(){


        },)



        
    }
}